#!/usr/bin/env bash
# Quill Programming Language Launcher (Unix/Linux/macOS)
# Usage: quill program.quill

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/core/quill.py" "$@"
